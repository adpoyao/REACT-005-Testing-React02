import React from 'react';
import {shallow, mount} from 'enzyme';
import GuessSection from './guess-section';

describe('<GuessSection />', () => {
    let wrapper;

    it('should not cause a pandemic', () => {
      shallow(<GuessSection />);
  })
})